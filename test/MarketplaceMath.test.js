import { expect } from "chai";
import { ethers } from "hardhat";

describe("MarketplaceMath", function () {
  it("Should calculate fee", async function () {
      // Library test stub
    expect(true).to.be.true;
  });
});
